# PostgreSQL connection (required in production for persistent hunts)
DATABASE_URL=postgres://user:password@hostname:5432/dbname

# Public site URL used in canonical, Open Graph, sitemap, and robots.txt
SITE_URL=https://shiny-hunt-tracker.fly.dev

PORT=3000
NODE_ENV=production
